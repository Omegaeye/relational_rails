<form action="/heroes/<%= @hero.id %>/mounts" method="POST">
  <input type="hidden" name="authenticity_token" value="<%= form_authenticity_token %>">
  <p>Enter a new mount:</p>
  <input type='text' name='mount[name]'/><br/>
  <input type='submit'/>
</form>
