<%= form_with url: "/mounts/#{@mount.id}", method: :patch, local: true do |f| %>
  <%= f.label :name %>
  <%= f.text_field :name %>
  <%= f.label :fly? %>
  <%= f.text_field :fly %>
  <%= f.label :level %>
  <%= f.number_field :level %>
  <%= f.submit 'submit' %>
<% end %>
