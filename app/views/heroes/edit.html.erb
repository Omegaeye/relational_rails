<%= form_with url: "/heroes/#{@hero.id}", method: :patch, local: true do |f| %>
  <%= f.label :name %>
  <%= f.text_field :name %>
  <%= f.label :alive? %>
  <%= f.text_field :alive %>
  <%= f.label :level %>
  <%= f.number_field :level %>
  <%= f.submit 'Update Hero' %>
<% end %>
